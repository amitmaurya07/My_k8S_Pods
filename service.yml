apiVersion: v1
kind: Service
metadata:
  name: myginx-service
  labels:
    app: mynginx
spec:
  type: NodePort
  selector:
    app: mynginx
  ports:
  - port: 80
    nodePort: 30001
    protocol: TCP
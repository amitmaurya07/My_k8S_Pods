apiVersion: v1
kind: Service
metadata:
  name: myginx-service
  labels:
    app: mynginx
spec:
  type: NodePort
  selector:
    app: mynginx
  ports:
  - port: 80  #Container Port Number
    nodePort: 30001 #Access Port Number
    protocol: TCP